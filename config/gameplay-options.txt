{
    DEFAULT AIM MODE IS:
    "crosshair" — weapon aims at camera ray hit point on world/NPC geometry

    OTHER AIM MODES:
    "world_hit" — weapon aims along muzzle ray, hitscan style
    "farpoint"  — weapon always aims at a distant point along camera forward

    FARPOINT MODE:
    Ignores world geometry for aiming direction.
    Weapon always points at camera_forward * farpoint_distance.
    Hitscan/projectile collision still works normally.
    Useful for: stable aiming, horizon aiming, avoiding weapon wobble on walls.

    farpoint_distance: 1000000.0 (configurable, hot-reloaded from gameplay.json)

    DASH MODES:
    "glide" = slide mode
    "tf2" = oldskool harder

    7 8 2026
}
