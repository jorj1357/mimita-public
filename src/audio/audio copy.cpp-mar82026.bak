// // C:\important\quiet\n\mimita-priv-v7\src\audio\audio.cpp
// /**
//  * purpose
//  * functions so we plau adio 
//  * and it sound good
//  * i think feb 10 2026 was date idk 
//  * i wrote dat mar 7 2026
//  * also its messy with comments about caht gpt 
//  * and the sound pool and sound queue and stuff
//  * but i think right now i idont reallu even care
//  * i like the fucked up broken bullshti version of it 
//  * it gives it flavor 
//  * no polished triple AAA game studio here i hate the polished nice clean
//  * i want dirty real raw
//  */

// // audio.cpp
// // THIS IS THE ONL PLACE THAT MINIAUDIO_IMPLEMENTATION CAN BE DEFINED 
// #define MINIAUDIO_IMPLEMENTATION
// #include "miniaudio.h"

// #include "audio.h"
// #include <string>
// #include <cstdlib>
// #include <cstdio>

// static ma_engine gEngine;
// static bool gAudioInit = false;

// static void initAudioOnce() {
//     if (gAudioInit) return;

//     ma_result r = ma_engine_init(NULL, &gEngine);
//     if (r != MA_SUCCESS) {
//         printf("[AUDIO] Failed to init engine\n");
//         return;
//     }

//     gAudioInit = true;
//     printf("[AUDIO] Engine initialized\n");
// }

// void playSound(const std::string& name, float volume) {
//     initAudioOnce();

//     std::string path = "assets/sound/" + name + ".wav";
//     printf("[AUDIO] play %s\n", path.c_str());

//     ma_engine_play_sound(&gEngine, path.c_str(), NULL);
// }

// // version 2 mar 7 2026
// // not using it mar 7 2026 this sound works and plays and idc so whatever 
// // chat gpt said to do this and idk what im doing so ill do it 
// // That version has a hidden issue:

// // ma_sound sound;

// // is destroyed when the function exits.

// // Miniaudio keeps the sound alive internally 
// // for a bit, but if you start doing lots of
// //  sounds later (weapons, impacts, etc.),
// //   you will want a sound pool.
// // void playSound(const std::string& name, float volume)
// // {
// //     initAudioOnce();

// //     std::string path = "assets/sound/" + name + ".wav";
// //     printf("[AUDIO] play %s\n", path.c_str());

// //     ma_engine_play_sound(&gEngine, path.c_str(), NULL);
// // }

// void playSoundAt(const std::string& name, glm::vec3 pos, float volume) {
//     initAudioOnce();

//     std::string path = "assets/sound/" + name + ".wav";

//     ma_sound sound;
//     if (ma_sound_init_from_file(&gEngine, path.c_str(), 0, NULL, NULL, &sound) == MA_SUCCESS) {
//         ma_sound_set_position(&sound, pos.x, pos.y, pos.z);
//         ma_sound_start(&sound);
//     }
// }

// // also version 2 mar 7 2026
// // void playSoundAt(const std::string& name, glm::vec3 pos, float volume)
// // {
// //     initAudioOnce();

// //     std::string path = "assets/sound/" + name + ".wav";

// //     ma_engine_play_sound(&gEngine, path.c_str(), NULL);
// // }

// void playRandomFootstep() {
//     int r = rand() % 4;
//     switch (r) {
//         case 0: playSound("entity/player/walk1"); break;
//         case 1: playSound("entity/player/walk2"); break;
//         case 2: playSound("entity/player/walk3"); break;
//         case 3: playSound("entity/player/walk4"); break;
//     }
// }
