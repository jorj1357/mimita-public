// miniz_export.h — static-build export macros for miniz.
// miniz.h includes this file; it is generated by CMake normally, so we provide
// it ourselves. The launcher compiles miniz into the EXE (not a DLL), so all
// export macros expand to nothing.
#ifndef MINIZ_EXPORT_H_INCLUDED
#define MINIZ_EXPORT_H_INCLUDED
#define MINIZ_EXPORT
#endif
