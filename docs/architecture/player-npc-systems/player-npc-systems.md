# Player and NPC Philosophy

9 2 2026 needs expansion but, 

humans and npcs both call the same exact paths for doing things in the game. 
humans and npcs both move using the same exact code, aim using the same code, fire, jump, dash, down dash, freeze, etc.
all the code should work seamlessly and regardless of  if its a npc or a human or an AI doing it or a TAS bot doing it. it just should work.
a player = a npc , and a npc = a player