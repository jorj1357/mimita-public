# Terminal Commands

9 2 2026 needs expansion, but THE ENTIRE game should be playable thru console commands only.

maybe tick? maybe frame?
some things are per frame, like, inputs should not wait for ticks to  update, it should be instant as possible to walk forward/move/shoot etc
server and client thinks in ticks, but inputs are sent super quick, per frame

like, walkforward = walks forward for 1 tick
walkbackward = back for 1 tick
jump = jumps asap

as well as chat, or exit game, or new gamemode, or leave server, or login or log out or change avatar etc etc

like first, make it work using terminal commands, and then a GUI comes from that working terminal command, so all we do is just visualize the commands u would be running anyway 