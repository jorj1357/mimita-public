# Muzzle flash migrated to hot effect policy

Date: 2026-09-15 23:00 EST (UTC 2026-09-16T03:00:00Z)
Branch: `8292026stash` (worktree, uncommitted)
Result: `PASS_WITH_HUMAN_REVIEW`

## SUBSYSTEM
Client effects - muzzle flash.

## REAL SHIPPING PATH MIGRATED
`EffectPartSystem::spawnMuzzleFlash` is the single cold owner called by every
weapon-fire path (revolver, rocket, grenade, hitscan via `weapon-fire-hit.cpp`,
network `multiplayer-shots.cpp`/`multiplayer-projectiles.cpp`/`engine-tick-net.cpp`).
It now emits a generic `effect.request` (`effect.muzzle`) carrying the tool key as
a runtime hash; the hot `hot.effect-composition` policy composes a generic flash
effect entity and sets `handled = 1`, so the cold composition (and its dynamic
light block) yields. Exactly one owner.

## COLD OWNER REMOVED
Muzzle-flash visual composition leaves `EffectPartSystem::spawnMuzzleFlash`
(fallback only).

## HOT OWNER ADDED
`effect.muzzle` branch in `modules/presentation/effect-composition.cpp` (generic
effect entity, short lifetime). No weapon-type branch.

## GENERIC PRIMITIVE USED/ADDED
Reuses `EffectRequestV1` + `effect.request` + hot `effect-lifecycle`. No new ABI.

## COLD MECHANISM REMAINING
`EffectPartSystem` particle/light draw (the flash entity is a generic effect
entity drawn through the existing `render.mesh`/debug fill paths; the dynamic
light block is skipped only when hot handles).

## COMPATIBILITY FALLBACK
Cold `spawnMuzzleFlash` composition when no hot handler handles the fact.

## RUNTIME-UNKNOWN PROOF
The tool key is `gameHash(weaponId)` (runtime hash); `hoteffect`/`hotsurfaceeffect`
prove runtime-new effect types. A runtime tool's fire fact reaches the same path
with no enum.

## SELFTEST PROVEN
"real weapon-fire fact reaches the hot muzzle policy (runtime tool key, no enum)";
full suite PASS.

## LIVE HOT-EDIT PROVEN
No (no screen).

## CONCURRENCY BOUNDARY STATUS
Clean - no movement/network files touched.

## BUGS DEFERRED
Screen flash, camera shake; muzzle art/light; remaining audio policy; weapon
presentation; tuning.

## WOULD THIS BUG STILL REQUIRE COLD RESTART?
Muzzle-flash behavior: **no** (hot). Screen-flash/camera-shake/weapon-fire-audio/
weapon-presentation: yes until migrated. Renderer/backend bugs stay cold.

## NEXT COLD OWNER
Camera/screen effect policy (generic camera-effect + screen-effect commands), then
weapon-fire audio, then footstep audio, then weapon presentation.

## Files changed
`src/effects/effect-part.cpp`,
`src/hot-reload/modules/presentation/effect-composition.cpp`,
`src/network/hot-combat-selftest.cpp`,
`docs/architecture/live-development/hot-cold-audit.md`,
`docs/architecture/live-development/hot-kernel-next-steps.md`.
