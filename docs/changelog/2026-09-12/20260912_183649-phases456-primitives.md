# Phases 4-6 primitives: per-source builds, packages, capabilities, domains, network agreement

- EST timestamp: 2026-09-12 14:36:49 EDT (UTC 2026-09-12T18:36:49Z)
- Branch: `8292026stash`
- Commits: none (uncommitted for human review)
- Result: `PASS_WITH_HUMAN_REVIEW` (primitives pass; one bootstrap cold build pending)

## Task

Implement phases 4-6 of the general live-runtime plan and explain what remains.

## Phase 4 — packages, assets, subsystems

- `build_game_dll.py`: the hot build now compiles each source separately with
  `-MMD` (per-TU `.d` under the generation `obj/` dir) and then links, so the
  `.d` dependency graph can drive incremental rebuilds. Verified: a live build
  produced `actor-behavior.o.d`, `effect-part.o.d`, `presentation.o.d`,
  `rocket-behavior.o.d`.
- `src/project/live-runtime.*`: `PackageRegistry` (tree `package.json`),
  `ResourceRegistry` (content-addressed assets), `SubsystemSlot` lifecycle
  (`initialize -> mirror -> validate -> switch -> retire`), and
  `ComponentSchemaRegistry`.
- `src/project/project-watcher.*`: recursive `ReadDirectoryChangesW` watcher,
  wired into `HotReloadSystem` (watcher dirty forces an immediate source/hash
  check; the hash scan remains the source of truth).
- `ProjectControl` now initialized on the dedicated server too.

## Phase 5 — multiplayer distribution

- `src/network/packets.h`: `CodeGenerationPacket` extended with
  `logicalCodeHash`, `platformPackageHash`, `moduleSetHash`, and `phase`
  (status/READY/SWITCH); size guard raised to 96 bytes.
- `ServerPlayer` stores `reportedCodePhase`, `reportedLogicalCodeHash`,
  `reportedPlatformPackageHash` (`server.h`, `server-packets.cpp`).
- Client sends generation/hash + READY (`multiplayer-tick.cpp`); server announces
  the switch generation/hash and tick on activation and sets its own phase
  (`server.cpp`); client stores phase/logical/platform (`multiplayer-tick.cpp`,
  `multiplayer-context.h`).

## Phase 6 — security, authoring, generality

- `src/project/capabilities.*`: capability enum, grant/request/denial, `allows`.
  Not yet enforced at every call site (documented).
- `src/project/live-runtime.*`: `DependencyResolver` verifies local artifacts by
  content hash (`Verified`/`NotLocal`/`HashMismatch`); download/sandbox is later.
- `src/sim/domain-scheduler.*`: multiple simulation domains at independent rates
  (`gameplay.60`, `solver.600` tested); the 60 Hz rule becomes one domain.
- `src/project/world-hash.*`: deterministic world-state hashing over identity and
  known components.
- Agent control socket + terminal surface already present from Phase 0.

## Tests and evidence

- `python devscripts/live-build.py` -> generation 4, immutable file, `mimita.exe`
  untouched; per-source `.d` files produced.
- Standalone phase 4-6 harness -> `[PHASE456 SELFTEST] PASS` (10/10): domain
  scheduler, package discovery, resource scan, capability grant/deny,
  dependency verification, subsystem lifecycle, component schema, world hash
  determinism, recursive watcher.
- Standalone project harness -> `[PROJECT SELFTEST] PASS` (18/18).
- `-fsyntax-only` clean for all new/changed TUs (`capabilities.cpp`,
  `live-runtime.cpp`, `world-hash.cpp`, `project-watcher.cpp`,
  `phase456-selftest.cpp`, `domain-scheduler.cpp`, `hot-reload-system.cpp`,
  `server.cpp`, `server-packets.cpp`, `multiplayer-tick.cpp`, `game-cli.cpp`).
- Cold build: `build_agent.py` refused with `HOT_RELOAD_BOUNDARY_VIOLATION`
  (two `mimita.exe` running).

## What remains (after the cold build)

1. Behavior-table ABI (per-behavior id/hash) + per-entity binding dispatch.
2. Assets/shaders/configs in the project tree with generation-stamped loaders.
3. Run declared schema migrations at activation (registry exists).
4. Full READY/switch-tick handshake with delayed activation at tick N.
5. Capability enforcement at call sites; agent auth on the control socket.
6. Dependency download/sandbox/promote pipeline.
7. Inspector for entities/components/bindings/generations/hashes.
8. Schema-driven components/events, hierarchical spaces, GPU/CPU compute
   capabilities, and arbitrary real-time domain generality.

## Files

New: `src/project/capabilities.h/.cpp`, `live-runtime.h/.cpp`,
`world-hash.h/.cpp`, `project-watcher.h/.cpp`, `phase456-selftest.h/.cpp`,
`src/sim/domain-scheduler.h/.cpp`.

Changed: `build_game_dll.py`, `src/project/project-layer` docs, `src/hot-reload/
hot-reload-system.h/.cpp`, `src/network/packets.h`, `src/network/server.h`,
`src/network/server.cpp`, `src/network/server-packets.cpp`,
`src/network/multiplayer-tick.cpp`, `src/network/multiplayer-context.h`,
`src/game/game-cli.cpp`,
`docs/architecture/live-development/project-layer.md`,
`docs/architecture/live-development/hot-kernel-next-steps.md`.

## Pre-existing edits preserved

The developer's `rocket-behavior.cpp` proof value and all unrelated working-tree
changes were preserved.
