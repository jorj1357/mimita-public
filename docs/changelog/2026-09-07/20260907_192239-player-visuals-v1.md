# Player visuals v1 implementation

- Branch: current working branch (not changed by this session)
- Commit: not created by this session
- Timestamp: 2026-09-07T19:22:39Z
- Task: implement v1 of `docs/specs/visuals/visuals.md` for configurable player outlines.

## Pre-existing changes

The worktree already contained unrelated modified and untracked files before this task, including website files, account/analytics/healthbar configuration, `docs/regressions/regressions-v1.md`, replay changelogs/gold material, and prior engine changes. None of those files were intentionally edited for this task.

## Changes made

- Added `config/playervisuals.json` with independent `self`, `enemy`, and `teammate` outline settings.
- Added `src/config/player-visuals-config.h` and `src/config/player-visuals-config.cpp` for loading, validating, atomically applying, and hot-reloading the player visual state.
- Added the config poll to `src/engine/engine-tick-setup.cpp` under the existing master hot-reload gate.
- Extended the shared player renderer to select self, teammate, or enemy settings and apply the outline to existing players immediately after reload.
- Added an inverted-hull outline pass using existing player geometry, normal expansion, front-face culling, alpha blending, depth-write suppression, and OpenGL-state restoration.
- Added `uOutlinePass` and `uOutlineThickness` to `shaders/basic.vert`.
- Passed the local player's match team from `src/engine/engine-tick-render.cpp`.
- Added rate-limited centralized Render diagnostics.

## Exact live configuration paths

- `config/playervisuals.json:renderOrder`
- `config/playervisuals.json:self.outline.enabled`
- `config/playervisuals.json:self.outline.thickness`
- `config/playervisuals.json:self.outline.alpha`
- `config/playervisuals.json:self.outline.color`
- `config/playervisuals.json:self.outline.visibleThroughWalls`
- `config/playervisuals.json:self.outline.disappearOnDeath`
- `config/playervisuals.json:self.outline.renderOrder`
- The same `outline.*` paths under `enemy` and `teammate`.

## Validation

- `config/playervisuals.json` parsed successfully with the expected enemy, teammate, and color values.
- `git diff --check` completed without content errors.
- `python build_agent.py` completed with `Status: SUCCESS` and relinked the canonical `C:\mimita-priv-v8\mimita.exe`.
- Focused behavior, efficiency, and logging reviews were applied before completion.

## Remaining human review

The build and source paths are verified, but visual acceptance still requires a human run covering moving/dashing/jumping players, wall occlusion, through-wall visibility, thickness and color edits on existing players, death behavior, multiple players, and malformed-then-corrected JSON.

Competitive policy override and a dedicated terminal inspection command were not added in this pass because `config/competitive.json` is currently only a placeholder and no existing competitive visual-policy owner was present to extend safely. Local preferences and renderer-side category behavior are implemented.
