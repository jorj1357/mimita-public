# Clean afad20a checkout build

Time: 2026-09-25 21:33:23 EDT
Branch: afad20a-rebuild
Commit: afad20a6296d38bde7a5abf685f045d1d3e2a3b6

## Request

Build the clean `afad20a` checkout in `C:\mimita-v9` with
`build-quick-test-using-build-python-v1.bat` and `buildv2.py`, fixing blockers
until the executable is created and starts.

## Documents read

- `AGENTS.md`
- `docs/ROUTER.md`
- `docs/operations/build-and-exe/build-and-exe.md`
- `docs/operations/task-completion/task-completion.md`
- `docs/regressions/regressions-v1.md`

## Findings and changes

The first build failed because the cloned Git submodule `external/libjuice` was
present only as an empty submodule directory. The compiler reported:
`fatal error: juice/juice.h: No such file or directory`.

No game source code was changed. The required submodule was initialized from
`.gitmodules` at commit `3ec54cf5f08e0ec12b6b941506680eabe15cdb44`.

The checkout does not contain the private developer toolchain or GLFW folders.
For this local build, the existing machine dependencies were supplied through
environment variables:

- `MIMITA_COMPILER=C:\important\msys64\mingw64\bin\g++.exe`
- `MIMITA_GLFW_INCLUDE=C:\important\glfw-3.4.bin.WIN64\include`
- `MIMITA_GLFW_LIB=C:\important\glfw-3.4.bin.WIN64\lib-mingw-w64`

## Build evidence

`python buildv2.py` completed compilation and linking after submodule
initialization. The executable was created at:

`C:\mimita-v9\mimita.exe`

Recorded size: 108,366,104 bytes.

The build output reached `[LINK] mimita.exe` and reported:

`[BUILD] compiled on Sep 25 2026 at 21:28:19`

## Runtime evidence

The executable started and reached:

- OpenGL 3.3 initialization
- shader compilation and program linking
- engine initialization
- UI initialization
- avatar loading
- network configuration loading
- hot-reload DLL loading
- `reload success generation=1`

The game process was observed during startup and later exited. No source
compile or link error remained after the submodule was initialized.

## Human review still needed

The build and startup are proven. Actual gameplay, movement, multiplayer, and
ragdoll behavior still require human playtesting in this fresh checkout.

The launch also updated `config/analytics.json`, and the compiler regenerated
`src/pch.h.d`; these are generated/runtime changes and were not intentional
feature edits.
