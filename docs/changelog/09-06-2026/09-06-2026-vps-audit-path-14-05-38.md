// 09 06 2026, 14 05 EST
/* purpose
* record the read-only VPS audit documentation path added for future agents
* connect the audit procedure to the documentation router
* preserve the exact scope and validation of this documentation-only session
* this file DOES NOT record code or database changes
* this file DOES NOT contain credentials or private production data
* this file DOES NOT claim live schema access was completed
*/

# VPS Audit Path Changelog

## Final state

Added `docs/operations/vps-audit.md` and routed VPS inspection tasks to it from
`docs/ROUTER.md`.

The new document defines read-only commands and evidence requirements for:

- VPS host, services, ports, PM2, and deployed Git state
- PostgreSQL table, column, constraint, index, and migration inspection
- aggregate database values for persistent progression
- user/stats consistency counts without exposing IDs
- backup metadata
- targeted log inspection with secret redaction
- one-hour temporary read-only database roles
- changelog evidence for future repository-touching sessions

## Exact files changed by this session

`docs/operations/vps-audit.md` was newly added with 215 lines. It contains the
full read-only audit procedure, including the database schema and aggregate
queries requested by the data-saving investigation.

`docs/ROUTER.md` received one new routing row at line 77:

```text
| VPS inspection or repository-to-production comparison | `docs/operations/vps-audit.md`, `docs/operations/vps-deployment/vps-deployment.md`, `docs/operations/task-completion/task-completion.md` |
```

## Reasoning

The existing VPS deployment document described deployment boundaries but did not
provide a reusable, sanitized path for comparing the live database and deployed
services with the local repository. The new audit document keeps inspection
separate from deployment and prohibits credential, token, and private-row
exposure.

## Pre-existing changes

The worktree already contained unrelated modified, deleted, and untracked
documentation files before this session. They were preserved and not rewritten.
The pre-existing untracked changelog
`docs/changelog/09-06-2026/09-06-2026-json-architecture-09-48-53.md` was not
modified.

## Branch and commit

Branch: `8292026stash`

HEAD at inspection: `1b3eb8f`

No commit was created by this session.

## Documents used

- `AGENTS.md`
- `docs/ROUTER.md`
- `docs/operations/vps-deployment/vps-deployment.md`
- `docs/operations/task-completion/task-completion.md`
- `docs/specs/data-saving/data-saving.md`
- `docs/regressions/regressions-v1.md`

## Validation

- `git diff --check` ran; it reported only pre-existing trailing whitespace in
  unrelated documentation files.
- Confirmed the new router entry exists at `docs/ROUTER.md:77`.
- Confirmed the new audit document exists and contains 215 lines.
- No build was run because no code changed.
- No database writes, VPS file writes, deployments, migrations, or service
  restarts were performed.

## Remaining human review

The live PostgreSQL schema and aggregate values still need to be collected using
the new procedure. The earlier audit confirmed PostgreSQL was online but could
not authenticate through the attempted `sudo` path. Future agents should use
the deployed environment credentials on the VPS or a temporary one-hour
read-only role, without copying the password into chat.
