# Stage B3-B5 + Stage C1: attack gates, dead-code removal, kill attribution, NPC targeting

Date (UTC): 2026-09-23T18:40:00Z
Status: implemented; cold build and selftests verified; hot providers resolved and exercised

## Scope

Finish the tractable Stage B combat-policy slices and start Stage C (server
actor/lifecycle) with the NPC targeting policy.

## Changes

### B3 — hot attack gates (`net.attack-gates`)
- New `src/hot-reload/hot-attack-gates.h` (POD `GameAttackGatesV1` + shared
  `evaluate`) and `src/hot-reload/modules/attack-gates-policy.cpp` (provider).
- `src/network/server-attack.cpp`: the cooldown-grace check, the per-tick shot
  rate limit, and the hitscan origin geometry tolerance now come from the hot
  policy. The two cold call sites (cooldown/rate before the hot tool dispatch,
  geometry after it) are preserved in order.

### B4 — server-projectiles dead-code removal
- Deleted the `#if 0` legacy projectile-fire block (~390 lines) and the uncalled
  `handleGenericProjectileAttack` (~249 lines) from
  `src/network/server-projectiles.cpp` (2210 -> 1455 lines). No callers existed.

### B5 — hot NPC kill attribution (`net.kill-attribution`)
- New `src/hot-reload/hot-kill-attribution.h` (POD `GameKillAttributionV1` +
  shared `evaluate`); provider registered from
  `modules/damage-application-policy.cpp`.
- `src/network/server-damage.cpp`: the NPC kill-reattribution window and decision
  in `queueServerDamageConfirmedEvent` are now hot.

### C1 — hot NPC targeting (`net.npc-targeting`)
- New `src/hot-reload/hot-npc-targeting.h` (POD hostility + candidate-score
  requests + shared implementation) and
  `src/hot-reload/modules/npc-targeting-policy.cpp` (provider).
- `src/network/server-npcs.cpp`: `actorsAreHostile` and the candidate
  `scoreCandidate` formula now resolve the hot policy. The candidate loops, the
  stickiness addition, the switch-threshold application, and the typed target
  projection remain cold.

### Manifest
- Added the four new headers to `headers` in `hot-modules.json`.

## Evidence

- Source changes: `hot-attack-gates.h`, `hot-kill-attribution.h`,
  `hot-npc-targeting.h` (new); `modules/attack-gates-policy.cpp`,
  `modules/npc-targeting-policy.cpp` (new); `modules/damage-application-policy.cpp`;
  `network/server-attack.cpp`, `network/server-damage.cpp`,
  `network/server-npcs.cpp`, `network/server-projectiles.cpp`,
  `live-code/live-code-selftest.cpp`, `hot-reload/hot-modules.json`.
- Build (source/build evidence): `python build_agent.py` -> `Status: SUCCESS`,
  `mimita-20260923T183426.exe`.
- Automated tests (test evidence): `--live-code-selftest` -> PASS, including
  `[CAPABILITY_RESOLVED]` for `net.attack-gates`, `net.kill-attribution`,
  `net.npc-targeting` and their new checks.
- Runtime / human acceptance: pending.

## Remaining Stage B

- `server-projectiles.cpp`: splash/explode/area-effect/impact policy.
- `server-attack.cpp`: client hit-claim acceptance ("shoot what I saw").
- `server-physical-contact.cpp`: contact detection/episode classification policy.

## Stage C status

Started: NPC hostility + target scoring are hot. Remaining: NPC respawn policy,
NPC combat/kill attribution and pellet/effect flags; player spawn/respawn,
broadcast interpolation, and rewind policy.
